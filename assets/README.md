# assets

Drop the following images here for production builds:

- `icon.png` (1024×1024)
- `splash.png`
- `adaptive-icon.png` (Android)
- `favicon.png` (web)

Expo's CLI generates placeholders if they are missing during `expo prebuild`.
